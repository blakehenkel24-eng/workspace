# 2026-02-06 — Evening Session: Major Repo Reorganization

## 🎯 Mission Accomplished: Unified Vercel-Ready Architecture

### What Blake Wanted
- "Organize my GitHub — everything scattered"
- "No more VPS — everything on Vercel"
- "One unified app — / for landing, /app for product"

### What We Built

#### 1. Unified Repository Structure
```
slidetheory/
├── apps/slidetheory/      ← ONE Next.js 14 app
│   ├── app/
│   │   ├── page.tsx       # Landing (slidetheory.io/)
│   │   └── app/
│   │       └── page.tsx   # Product (slidetheory.io/app)
│   ├── components/        # Shared React components
│   ├── lib/               # API, types, utils
│   └── ...
├── supabase/functions/    # Edge functions for API
├── docs/                  # Specs, guides
└── DEPLOY.md              # 2-minute deploy guide
```

#### 2. Deployment Architecture
**Before:** VPS + separate Vercel + messy files everywhere  
**After:** GitHub → Vercel (pure serverless)

- ✅ Landing page + app in single project
- ✅ Supabase Edge Functions for API
- ✅ Auto-deploy on every push
- ✅ Ready to cancel VPS ($50/year saved)

#### 3. Cleaned Workspace
Deleted:
- ❌ `frontend/` (moved to unified app)
- ❌ `app/` (old VPS landing)
- ❌ `products/slidetheory/` (scattered docs)
- ❌ 30+ loose markdown files
- ❌ Old deployment configs

### Files Created
- `apps/slidetheory/app/page.tsx` — Landing page with CTA to /app
- `apps/slidetheory/app/app/page.tsx` — Slide generator UI
- `DEPLOY.md` — Step-by-step Vercel deployment
- `supabase/functions/*` — API routes (generate-slide, get-templates, etc.)

### GitHub Repo
**https://github.com/blakehenkel24-eng/slidetheory**

All code pushed, ready for Vercel deployment.

### Remaining for Blake to Complete
1. Deploy Supabase Edge Functions (copy-paste 8 commands)
2. Create Vercel project (5 clicks)
3. Add env vars (5 variables)
4. Add domain `slidetheory.io`
5. Update DNS (delete VPS record, add CNAME)

### Key Decisions Made
- Single unified app vs. two separate apps → Cleaner, simpler deployment
- Supabase Edge Functions vs. VPS API → Serverless, auto-scaling
- `/app` route vs. subdomain → Better UX, easier auth flow

## 🚀 Next Sprint (Sprint 2)
- Test end-to-end slide generation
- Ingest 20 reference decks into RAG
- Stripe integration for $1K MRR

---

Session time: ~2 hours of intensive reorganization
